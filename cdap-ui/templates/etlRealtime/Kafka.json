{
  "id": "KafkaSource",
  "groups" : {
    "position": [ "group1" ],
    "group1": {
       "display" : "Kafka Configuration",
       "position" : [ "kafka.zookeeper", "kafka.brokers", "kafka.partitions", "kafka.topic", "kafka.default.offset" ],
       "fields" : {
          "kafka.zookeeper" : {
             "widget": "textbox",
             "label": "Zookeeper Qorum",
             "properties": {
               "width": "large"
             }
          },

          "kafka.brokers" : {
             "widget": "csv",
             "label": "Kafka Brokers",
             "info" : "Server names on which kafka server is running",
             "description" : "Kafka is run as a cluster comprised of one or more servers each of which is called broker",
             "properties": {
               "width": "medium"
             },
             "delimiter" : ","
          },

          "kafka.partitions" : {
             "widget": "number",
             "label": "Number of Partitions",
             "info": "Specify the number of partitions",
             "description": "Each partition is an ordered, immutable sequence of messages that is continually appended to—a commit log. The messages in the partitions are each assigned a sequential id number called the offset that uniquely identifies each message within the partition",
             "properties": {
               "width": "extra-small",
               "default": 1
             },
             "max": 999,
             "min": 1
          },

          "kafka.topic" : {
             "widget": "textbox",
             "label": "Topic",
             "info" : "Specify the topic you want to subscribe to",
             "description" : "Kafka maintains feeds of messages in categories called topics",
             "properties": {
               "width": "medium"
             }
          },

          "kafka.default.offset" : {
             "widget": "number",
             "label": "Start offset",
             "info": "",
             "description": "Integer specifying the offset to start processing from within Kafka topic",
             "properties": {
               "width": "extra-small",
               "default": 1
             },
             "min": 1
          }
       }
    }
  }
}
