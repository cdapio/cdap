{
  "id": "PythonEvaluator",
  "groups" : {
    "position": [ "group1" ],
    "group1": {
       "position" : ["script"],
       "fields" : {
          "script" : {
             "widget": "python-editor",
             "properties" : {
                "default" : "def transform(input, emitter, context):\n    emitter.emit(input)\n"
             }
          }
       }
    }
  },
  "schema" : {
     "widget": "schema",
     "schema-types" : [ "boolean", "int", "long", "float", "double", "bytes", "string", "map<string, string>" ],
     "schema-default-type" : "string"
  }
}
