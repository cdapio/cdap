{
  "id": "Table",
  "groups" : {
    "position": [ "group1" ],
    "group1": {
       "display" : "General",
       "position" : [ "name", "schema", "schema.row.field", "case.sensitive.row.field" ],
       "fields" : {
          "name" : {
             "widget": "textbox",
             "label": "Name",
             "description" : "Specifies the name of the table the data will be written to"
          },

          "schema" : {
             "widget": "schema",
             "label": "Schema",
             "description" : "Specify the schema for the table",
             "schema-types" : [ "boolean", "int", "long", "float", "double", "bytes", "string" ],
             "schema-default-type" : "string"
          },

          "schema.row.field" : {
             "widget": "textbox",
             "label" : "Row Field",
             "description" : "Specify the field from the above record to be used as key"
          },

          "case.sensitive.row.field" : {
             "widget": "select",
             "label" : "Case-sensitive Row Field",
             "properties" : {
               "values" : [ "true", "false" ],
               "default" : "true"
             },
             "description" : "Specify whether the row field specified in schema.row.field is case-sensitive"
          }
       }
    }
  }
}
