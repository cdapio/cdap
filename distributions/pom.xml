<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>parent</artifactId>
    <groupId>com.continuuity</groupId>
    <version>2.3.1</version>
    <relativePath>../parent/pom.xml</relativePath>
  </parent>

  <artifactId>distributions</artifactId>
  <packaging>jar</packaging>

  <profiles>

    <profile>
      <id>rpm-prepare</id>
      <build>
        <resources>
          <resource>
            <directory>${project.basedir}/src/etc</directory>
            <targetPath>${stage.dir}/etc</targetPath>
          </resource>
        </resources>
      </build>
    </profile>

    <profile>
      <id>deb-prepare</id>
      <build>
        <resources>
          <resource>
            <directory>${project.basedir}/src/etc</directory>
            <targetPath>${stage.dir}-deb/etc</targetPath>
          </resource>
        </resources>
      </build>
    </profile>

    <profile>
      <id>rpm</id>
      <build>
        <plugins>
          <!-- Distribution builds top level continuuity package. It builds differently than others -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>1.3.1</version>
            <executions>
              <execution>
                <id>rpm-package</id>
                <phase>package</phase>
                <goals>
                  <goal>exec</goal>
                </goals>
                <configuration combine.self="override">
                  <workingDirectory>${project.build.directory}</workingDirectory>
                  <executable>fpm</executable>
                  <commandlineArgs>
                    --name "continuuity"
                    --vendor "Continuuity Inc."
                    --maintainer "support@continuuity.com"
                    --license "Copyright 2012-2014 Continuuity,Inc. All Rights Reserved."
                    --description "Component of Continuuity Enterprise Reactor"
                    --category misc
                    -s dir
                    -t rpm
                    -C "${stage.dir}"
                    --version "${package.version}"
                    --architecture all
                    --url "http://www.continuuity.com/products"
                    --template-value "project=continuuity"
                    --after-install "${project.basedir}/src/rpm/scripts/common-postinst"
                    --before-install "${project.basedir}/src/rpm/scripts/common-preinst"
                    --before-remove "${project.basedir}/src/rpm/scripts/common-prerm"
                    --iteration 1
                    etc
                  </commandlineArgs>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- Extra deployment for rpm package. -->
          <!-- This has to be in child level, otherwise all modules would try to deploy. -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-deploy-plugin</artifactId>
            <version>2.8</version>
            <executions>
              <execution>
                <id>deploy-rpm</id>
                <phase>deploy</phase>
                <goals>
                  <goal>deploy-file</goal>
                </goals>
                <configuration>
                  <version>${project.version}</version>
                  <groupId>${dist.deploy.groupId}</groupId>
                  <artifactId>continuuity</artifactId>
                  <packaging>noarch.rpm</packaging>
                  <generatePom>false</generatePom>
                  <file>${project.build.directory}/continuuity-${package.version}-1.noarch.rpm</file>
                  <classifier>1</classifier>
                  <repositoryId>continuuity</repositoryId>
                  <url>${deploy.url}</url>
                </configuration>
              </execution>
            </executions>
          </plugin>

        </plugins>
      </build>
    </profile>

    <profile>
      <id>deb</id>
      <build>
        <plugins>
          <!-- Distribution builds top level continuuity package. It builds differently than others -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>1.3.1</version>
            <executions>
              <execution>
                <id>deb-package</id>
                <phase>package</phase>
                <goals>
                  <goal>exec</goal>
                </goals>
                <configuration combine.self="override">
                  <workingDirectory>${project.build.directory}</workingDirectory>
                  <executable>fpm</executable>
                  <commandlineArgs>
                    --name "continuuity"
                    --vendor "Continuuity Inc."
                    --maintainer "support@continuuity.com"
                    --license "Copyright 2012-2014 Continuuity,Inc. All Rights Reserved."
                    --description "Component of Continuuity Enterprise Reactor"
                    --category misc
                    -s dir
                    -t deb
                    -C "${stage.dir}-deb"
                    --version "${package.version}"
                    --architecture all
                    --url "http://www.continuuity.com/products"
                    --template-value "project=continuuity"
                    --after-install "${project.basedir}/src/debian/scripts/common-postinst"
                    --before-install "${project.basedir}/src/debian/scripts/common-preinst"
                    --before-remove "${project.basedir}/src/debian/scripts/common-prerm"
                    --iteration 1
                    etc
                  </commandlineArgs>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- Extra deployment for deb package -->
          <!-- This has to be in child level, otherwise all modules would try to deploy. -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-deploy-plugin</artifactId>
            <version>2.8</version>
            <executions>
              <execution>
                <id>deploy-deb</id>
                <phase>deploy</phase>
                <goals>
                  <goal>deploy-file</goal>
                </goals>
                <configuration>
                  <version>${project.version}</version>
                  <groupId>${dist.deploy.groupId}</groupId>
                  <artifactId>continuuity</artifactId>
                  <packaging>deb</packaging>
                  <generatePom>false</generatePom>
                  <file>${project.build.directory}/continuuity_${package.version}-1_all.deb</file>
                  <repositoryId>continuuity</repositoryId>
                  <url>${deploy.url}</url>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

  </profiles>

</project>