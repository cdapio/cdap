<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright © 2025 Cask Data, Inc.

  Licensed under the Apache License, Version 2.0 (the "License"); you may not
  use this file except in compliance with the License. You may obtain a copy of
  the License at

  http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
  WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
  License for the specific language governing permissions and limitations under
  the License.
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.cdap.cdap</groupId>
    <artifactId>cdap</artifactId>
    <version>6.12.0-SNAPSHOT</version>
  </parent>

  <artifactId>cdap-metadata-ext-spanner</artifactId>

  <properties>
    <!--
      All four versions below are a matched set: they are exactly what
      com.google.cloud:libraries-bom:${gcp-libraries-bom.version} resolves to.
      After bumping the BOM, re-derive them with:
        mvn -pl cdap-metadata-ext-spanner dependency:list -DincludeScope=runtime
      guava.version must be overridden here because the root pom manages
      com.google.guava:guava via ${guava.version} and would otherwise pin the
      much older version the Hadoop/Spark side of CDAP uses.
    -->
    <gcp-libraries-bom.version>26.88.1</gcp-libraries-bom.version>
    <spanner.version>6.122.0</spanner.version>
    <protobuf.version>4.33.6</protobuf.version>
    <guava.version>32.0.0-jre</guava.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!--
        Declared ahead of the libraries-bom import on purpose. google-cloud-spanner
        is compiled against the protobuf-java 4.x reflection API
        (com.google.protobuf.MapFieldReflectionAccessor, added in protobuf-java
        3.25/4.x). If anything on this module's classpath drags in an older
        protobuf-java, loading a Spanner gRPC stub dies with:
            java.lang.VerifyError: Bad return type ...
            com/google/spanner/v1/Session.internalGetMapFieldReflection
        Managing the version here makes it immune to transitive "nearest wins"
        resolution, which plain transitive scoping does not protect against.
      -->
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>${protobuf.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java-util</artifactId>
        <version>${protobuf.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.cloud</groupId>
        <artifactId>libraries-bom</artifactId>
        <version>${gcp-libraries-bom.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.cdap.cdap</groupId>
      <artifactId>cdap-metadata-spi</artifactId>
      <version>${project.version}</version>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>com.google.cloud</groupId>
      <artifactId>google-cloud-spanner</artifactId>
      <version>${spanner.version}</version>
    </dependency>
    <!--
      Explicit (not just transitive) so that the matching protobuf runtime is
      always copied into target/libexec, and therefore into the isolated
      extension directory in the master image.
    -->
    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java</artifactId>
    </dependency>
    <dependency>
      <groupId>io.cdap.cdap</groupId>
      <artifactId>cdap-common</artifactId>
      <version>${project.version}</version>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <profiles>
    <profile>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <version>3.8.1</version>
            <executions>
              <execution>
                <configuration combine.self="override">
                  <includeScope>runtime</includeScope>
                  <outputDirectory>${project.build.directory}/libexec</outputDirectory>
                  <overWriteIfNewer>true</overWriteIfNewer>
                  <overWriteReleases>false</overWriteReleases>
                  <overWriteSnapshots>false</overWriteSnapshots>
                  <prependGroupId>true</prependGroupId>
                  <silent>true</silent>
                </configuration>
                <goals>
                  <goal>copy-dependencies</goal>
                </goals>
                <id>copy-dependencies</id>
                <phase>prepare-package</phase>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <artifactId>maven-jar-plugin</artifactId>
            <executions>
              <execution>
                <configuration combine.self="override">
                  <finalName>${project.groupId}.${project.build.finalName}</finalName>
                  <outputDirectory>${project.build.directory}/libexec</outputDirectory>
                </configuration>
                <goals>
                  <goal>jar</goal>
                </goals>
                <id>jar</id>
                <phase>prepare-package</phase>
              </execution>
            </executions>
            <groupId>org.apache.maven.plugins</groupId>
            <version>2.4</version>
          </plugin>
        </plugins>
      </build>
      <id>dist</id>
    </profile>
  </profiles>
</project>
