package com.continuuity.test.data;

/**
 * Constants is a utility class that contains a set of constants that are used throughout the AppFabric project.
 */
public class Constants {
  /**
   * Corresponds to account id used when running in local mode.
   * NOTE: value should be in sync with the one used by UI.
   */
  public final static String DEFAULT_ACCOUNT_ID = "default";
} // end of Constants class
